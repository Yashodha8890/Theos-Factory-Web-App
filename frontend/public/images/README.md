Home hero carousel image slots:

- `home-carousel-1.jpg`
- `home-carousel-2.jpg`
- `home-carousel-3.jpg`
- `home-carousel-4.jpg`

If these files are present, the home page uses them first. If a file is missing, the app falls back to a remote event image so the carousel still renders.
